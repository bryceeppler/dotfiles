# ~/.zshenv — runs for EVERY zsh (login, interactive, and scripts).
# Managed in ~/dotfiles. Keep this minimal: anything heavy here slows down
# every non-interactive `zsh -c` call. PATH/login setup lives in zprofile;
# interactive setup (aliases, plugins, prompt) lives in zshrc.
